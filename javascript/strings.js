str="hello,man,who are you hello"
str1="you"

str2="how are "

// console.log(str.charAt(15))
// it dorsnot alows the use of negative index values and gives the letter present in that perticular location

// console.log(str.charCodeAt(15))
// gives the ASCII values of the letter 


// console.log(str.at(4))
// returns the letter present at that perticular index value which allows the use of negative index values 

// console.log(str.slice(5,8))
// cuts the string firts value is start point second value is ending point if we miss the second parameter then the whole string from that perticular position will be cutted
// if the parameter is in negative the count starts form ending

//  console.log(str.substring(7,5))
//  substring is similar to slice but substring returns the cutted string in string manner.if we not give second parameter it consists whole string 
// the secod parameter in this gives the length of the cuted string 

// console.log(str.toUpperCase())
// converts whole string to upper case

// console.log(str.toLowerCase())
// converts whole string to lower case if there any upper case it coverts whole lto lower case


// str3=str2.concat(str1)
// console.log(str3)
// adds two string an makes new string with the combination of both strings


// console.log(str.trim())
// removes the extra space from the given string in the begining of the string and end of the string  

// console.log(str1.trimEnd())
//removes the extra space from the given string in the end  of the string and end of the string   

// a=str1.padStart(9,"0")
// console.log(a)
// makes our string length equal to the length given as first parameter but adding the second parameter in the begining 

// a=str1.padEnd(7,"x")
// console.log(a)
// makes our string length equal to the length given as first parameter but adding the second parameter in the ending

// console.log(str1.repeat(4))
// /repeats the string until as per the given times 

// console.log(str2.replace("how","hai"))
// replaces the values according to the given parameters 

// console.log(str2.split(","))
// converts code into array

// b=str.replaceAll("hello","hai")
// console.log(b)
// replaces the values whereever present in the first parameter with the second value given as the second parameter 



    



















